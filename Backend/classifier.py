"""Groq signal classifier. Phase 1.6 / Phase 2."""

from __future__ import annotations


def classify_signal(competitor: str, raw_text: str, source_url: str) -> dict:
    """Classify a raw signal via Groq. Phase 1.6."""
    raise NotImplementedError("classifier.classify_signal — implement in Phase 1.6")
